#include<iostream>
using namespace std;
int main()
{
    // max min
    // int a[5],temp=0;
    // int *p = a;
    // for (int i = 0; i < 5; i++)
    // {
    //     cin >> p[i];
    // }


    // for (int i = 0; i < 5; i++)
    // {
    //     for (int j = i+1; j < 5;j++)
    //     {
    //         if(p[i]>p[j])
    //         {
    //             temp = p[i];
    //             p[i] = p[j];
    //             p[j] = temp;
    //         }
    //     }
    // }

    // cout << "\n";

    // for (int i = 0; i < 5; i++)
    // {
    //     cout << p[i]<<endl;
    // }

    //------------------------------------------------------------------

    // int a[5]={1,2,3,4,5},b[5];
    // int *p1 = a,*p2=b;

    // p2 = p1;

    // for (int i = 0; i < 5;i++)
    // {
    //     p2[i] = p1[i];
    // }

    // cout << endl;
    // for (int i = 0; i < 5; i++)
    // {
    //     cout << p2[i] << endl;
    // }

    //------------------------------------------------------------------

    // int a[5];
    // int *p = a;

    // for (int i = 0; i < 5;i++)
    // {
    //     cin >> p[i];
    // }

    // cout << "\n";
    // for (int i = 0; i < 5; i++)
    // {
    //     cout << p[i] << endl;
    // }

    //------------------------------------------------------------------

    // int a, b, temp;
    // int *p1 = &a,*p2=&b;

    // cin >> a;
    // cin >> b;

    // cout << "original: " << a << b << endl;

    // temp = *p1;
    // *p1 = *p2;
    // *p2 = temp;

    // cout << "swap: " << a << b << endl;

    //------------------------------------------------------------------

    // int a, b,sum;
    // int *p1=&a, *p2=&b;

    // cin >> a;
    // cin >> b;
    // sum = *p1 + *p2;
    // cout << a << endl;
    // cout << b << endl;

    // cout << *p1 << endl;
    // cout << *p2 << endl;

    // cout << "sum: " << sum;

    //------------------------------------------------------------------

    // int a = 10;
    // int *p = &a;

    // cout << a << endl;
    // cout << &a << endl;
    // cout << *p << endl;
    // cout << p << endl;
}